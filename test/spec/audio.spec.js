/* global chip8, describe, it, beforeEach, expect */

describe('Audio', function () {
    'use strict';

    it('should be defined', function () {
        expect(chip8.Audio).toBeDefined();
    });
});